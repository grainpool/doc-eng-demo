# relay-cli

Stub. The real `relay` CLI arrives in Phase 07: a `commander` program over the Relay HTTP API whose
`relay introspect --json` output — derived from the live commander tree, never hand-maintained —
is the T2_CLI product-truth authority source.
