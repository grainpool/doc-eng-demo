/**
 * Kept in lockstep with package.json "version" (asserted by a test so the two
 * cannot drift). Snapshots report it as relay_contracts_version.
 */
export const CONTRACTS_VERSION = "1.2.0" as const;
